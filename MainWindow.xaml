<Window x:Class="SecureBotWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CyberSecurity Bot"
        Height="600"
        Width="950"
        Background="#0F172A"
        WindowStartupLocation="CenterScreen">

    <Grid>

        <!-- Background Gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0F172A" Offset="0"/>
                <GradientStop Color="#111827" Offset="0.5"/>
                <GradientStop Color="#1E293B" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Main Border Container -->
        <Border Width="850"
                Height="520"
                CornerRadius="20"
                Background="#1E293B"
                Padding="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Orientation="Horizontal"
                            Margin="0,0,0,20">

                    <Image Source="/Screenshot 2026-05-27 151955.png"
                           Width="80"
                           Height="80"
                           Margin="0,0,15,0"/>

                    <StackPanel>

                        <TextBlock Text="CYBERSECURITY BOT"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="#38BDF8"/>

                        <TextBlock Text="Security Assistant"
                                   FontSize="14"
                                   Foreground="#CBD5E1"/>
                    </StackPanel>

                </StackPanel>
                <StackPanel Grid.Row="1"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,10,0,10">

                    <Button x:Name="btnAddTask"
            Content="Add Task"
            Width="100"
            Height="35"
            Margin="5"
            Click="btnAddTask_Click"/>

                    <Button x:Name="btnViewTasks"
            Content="View Tasks"
            Width="100"
            Height="35"
            Margin="5"
            Click="btnViewTasks_Click"/>

                    <Button x:Name="btnQuiz"
            Content="Quiz"
            Width="100"
            Height="35"
            Margin="5"
            Click="btnQuiz_Click"/>

                    <Button x:Name="btnLog"
            Content="Activity Log"
            Width="100"
            Height="35"
            Margin="5"
            Click="btnLog_Click"/>

                </StackPanel>

                <!-- Chat Area -->
                <Border Grid.Row="2"
                        Background="#0F172A"
                        CornerRadius="15"
                        Padding="15">

                    <ScrollViewer VerticalScrollBarVisibility="Auto">

                        <TextBlock x:Name="txtChat"
                                   Foreground="White"
                                   FontSize="15"
                                   TextWrapping="Wrap"
                                   FontFamily="Segoe UI"
                                   LineHeight="26"/>

                    </ScrollViewer>

                </Border>

                <!-- Bottom Input Area -->
                <Grid Grid.Row="3"
                      Margin="0,20,0,0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>

                    <!-- Message Box -->
                    <Border Background="#334155"
                            CornerRadius="12"
                            Margin="0,0,15,0">

                        <TextBox x:Name="message_txt"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="White"
                                 FontSize="15"
                                 Padding="15"
                                 CaretBrush="White"
                                 VerticalContentAlignment="Center"/>
                    </Border>

                    <!-- Send Button -->
                    <Button x:Name="send_btn"
                            Grid.Column="1"
                            Content="SEND"
                            Height="50"
                            FontWeight="Bold"
                            FontSize="15"
                            Foreground="White"
                            Background="#06B6D4"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="Button_Click">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="12">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>

                    </Button>

                </Grid>

            </Grid>

        </Border>

    </Grid>
</Window>